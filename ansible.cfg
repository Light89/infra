[defaults]
inventory = ansible/inventory/dev/hcloud.yml
remote_user = ansible
host_key_checking = False
retry_files_enabled = False
roles_path = ansible/roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
